# Repository review — 2026-09-17

Reviewed application base `1c2f50cb593ef9aaf1682c6b318860b256b7ef92` on Ubuntu.
This is an evidence record; the [task register](../tasks/README.md) owns all
follow-up work. Application fixes are deliberately left for their scoped tasks.

## Findings

| Priority | Finding and evidence | Follow-up |
| --- | --- | --- |
| P1 | Roster input can disable the guild dashboard. `roster.save` accepts `class: []`; the write returns 200 and subsequent state returns 500 when analytics hashes the class. Other unvalidated editable fields need the same boundary review. | [18 — roster validation](../tasks/todo/18-roster-field-validation.md); `guilds/modules/roster.py`, `guilds/modules/analytics.py` |
| P1 | Deactivated accounts are not denied by shared authorization. An existing user with `is_active=False` successfully created a reminder through `execute`. Discord commands/components refresh Access without checking that flag. Capture has an explicit check, illustrating the inconsistent boundary. | [19 — inactive accounts](../tasks/todo/19-inactive-account-authorization.md); `guilds/services.py`, `guilds/management/commands/runbot.py` |
| P1 | The compact Swarm deployment has no application scheduler. Live process inspection found bot and backup workers but no scheduler; readiness still passed. Automatic reminders, recurrence, scheduled summaries/sync and outbox delivery therefore require an external/manual tick. | [22 — scheduled work](../tasks/todo/22-swarm-scheduled-work.md); `examples/swarm-compose.yaml`, `scripts/container-entrypoint.sh` |
| P2 | An authenticated onboarding request containing JSON `[]` returns 500 because `.get` precedes object validation. This should be a useful 400 and no writes. | [20 — onboarding JSON](../tasks/todo/20-onboarding-json-validation.md); `guilds/views.py:onboard` |
| P2 | Creating a challenge exposes its pending roll in the action response, despite state-response redaction. Callers can inspect rolls before choosing which challenge to pursue. | [21 — challenge responses](../tasks/todo/21-pending-challenge-redaction.md); `guilds/modules/operations.py`, `guilds/services.py:response_for_role` |

The four application defects were reproduced with a freshly migrated temporary
SQLite database and synthetic accounts. No malformed input or disabled-account
experiment was sent to the live application. For roster/onboarding, the Django
test client captured HTTP statuses; the account and challenge reproductions
used the shared `execute` entry point. The Discord inactive-account consequence
is supported by source inspection, not a live Discord experiment.

## Coordination with existing work

These findings overlap active tasks; their owner documents were left intact:

- [15 — embedded bot](../tasks/in-progress/15-embedded-discord-bot-runtime.md):
  root `compose.yaml` still defines a separate `discord` bot profile and passes
  `DISCORD_SYNC_GLOBAL`/`DISCORD_SYNC_GUILD` only to that service, not `web`.
  The documented embedded-bot setup therefore does not receive those settings
  under root Compose, and enabling both runtimes starts duplicate bots. The
  examples already forward the settings. `integration_status.py` also still
  tells operators to enable removed bot/backup profiles. Finish these checks
  within task 15, coordinating examples with task 13.
- [06 — restore rehearsal](../tasks/in-progress/06-backup-restore-and-upgrade-rehearsal.md):
  a successful backup heartbeat is not restore/failover evidence. Live service
  history showed different image digests under one commit-style tag. Preserve
  the deployed digest when rehearsing rollback; a tag alone does not establish
  which build ran. Coordinate reproducible image tagging with
  [13 — deployment examples](../tasks/in-progress/13-compose-and-swarm-deployment-examples.md).
- [16 — response formatting](../tasks/complete/16-discord-readable-command-responses.md):
  its historical autocomplete timeout reproduced inside the sandbox, but the
  same isolated test passed outside it in 0.010 seconds. Do not file an
  application hang on that evidence. Full unsandboxed tests also passed.

Missing user-facing guild portability and Critiq XLSX import are already covered
by [11](../tasks/todo/11-guild-data-portability.md) and
[14](../tasks/todo/14-critiq-xlsx-import.md). Live Discord acceptance, current BDO
capture, regional OCR and human accessibility remain in tasks 03–09. This review
does not duplicate those tasks or equate synthetic coverage with acceptance.

## Documentation and workflow changes

- Corrected the README's obsolete claim that no bot had connected and documented
  the root Compose sync limitation while task 15 remains open.
- Marked READINESS as historical evidence instead of a competing claim queue.
- Corrected the database guide's service count, future-tense restore description,
  and transaction boundary: external preparation precedes the atomic commit.
- Clarified the missing scheduler in deployment docs and feature state.
- Added a short contributor loop: focused checks for routine changes, browser or
  integration checks when relevant, and the full gate for release validation.
  No CI, branch protection, dependencies or new tooling were added; code tasks
  still require Linux and Windows validation.
- Clarified that a branch-only claim is not visible on main. Publish one task PR
  when claiming and inspect open claims; new task planning and claiming can share
  a commit. Related review findings can share one planning PR.

## Validation and live observations

- Existing Django suite: **244 tests, OK, 8 skipped**, 12.509 seconds on Ubuntu
  Python 3.14 using disposable application storage outside the sandbox. The
  runner's system check reported no issues. Optional browser/PostgreSQL tests
  were not enabled. This is not a new 100% coverage or full release-gate result.
- JavaScript syntax checks passed for `app.js`, `onboard.js` and `insights.js`.
  Root Compose (jobs/discord profiles) and the compact Compose example rendered
  successfully using their example environment files. All 34 local Markdown
  links in the changed documents resolved; `git diff --check` passed.
- Sandbox run timed out at an async boundary after 180 seconds; the isolated
  autocomplete check timed out after 15 seconds. Outside the sandbox the isolated
  check and full suite passed. No application timeout fix is warranted by this.
- Read-only checks of the user-authorized deployment at approximately 00:48–00:52
  UTC: public HTTPS `/healthz/`, `/readyz/`, and `/login/` returned 200. Readiness
  reported database, migrations, storage and bot healthy, version `6e827933e3be`.
- Swarm reported `openiq_web` at 1/1, running on pi-3 for about 15 hours. Docker
  reported the container healthy. The running image digest was
  `sha256:f0feff4564f0a18a3a17a39ff7d791c3f8015005d539b937bd4c3578aa6ed2b0`;
  this is a different application revision from the reviewed main checkout.
- Bot heartbeat was about 20 seconds old; backup heartbeat was about three hours
  old against the documented four-hour interval. Worker inspection confirmed
  `runbot=True`, `backup_scheduler=True`, `scheduler=False`. No services were
  restarted, data changed, credentials printed or Discord messages sent.

Windows, live OAuth/Discord mutation workflows, a browser acceptance pass,
current-game capture, PostgreSQL integration, backup restoration and failover
were not exercised. Readiness is point-in-time evidence, not proof that those
workflows work. This review sampled the principal authorization, domain,
HTTP/UI, capture, backup/runtime and contributor paths; it is not a formal
security audit or a claim that no other defects remain.
